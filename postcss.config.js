/**
 * postcss.config.js
 * -----------------
 * PostCSS configuration for Tailwind CSS v4.
 * Uses @tailwindcss/postcss (v4 requires separate package).
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};
